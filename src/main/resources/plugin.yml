name: AllStarsParty
version: '1.0-SNAPSHOT'
main: be.artex.allStarsParty.AllStarsParty
api-version: '1.8.8'
commands:
  as:
    usage: /as <sub-command>
    description: "all All Stars Party's commands."
    aliases: [asp, allstars, allstartsparty]